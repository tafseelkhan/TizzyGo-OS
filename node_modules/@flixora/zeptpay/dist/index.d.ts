import { MerchantData, MerchantResponse } from "./types/merchantTypes";
import { EventEmitter } from "events";
export declare const sdkEvents: EventEmitter<any>;
export declare const ZeptPay: {
    flixora: {
        /**
         * ✅ CREATE MERCHANT
         * frontend publicKey + full merchant data + backend keys (clientKey + secretKey)
         */
        createMerchant(publicKey: string, fullData: MerchantData & {
            clientKey: string;
            secretKey: string;
        }): Promise<MerchantResponse>;
        /**
         * ✅ GET MERCHANT STATUS
         * Only token is needed
         */
        getMerchantStatus(token: string): Promise<any>;
    };
    events: EventEmitter<any>;
};
