"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createMerchant = createMerchant;
exports.getMerchantStatus = getMerchantStatus;
// sdk/merchantClient.ts
const axios_1 = __importDefault(require("axios"));
const sdkOption_1 = require("../options/sdkOption");
// 🔹 HTTP client with baseURL from SDK options
const httpClient = axios_1.default.create({ baseURL: sdkOption_1.defaultOptions.baseURL });
/**
 * ✅ CREATE MERCHANT
 * Calls company backend /api/merchant/create
 */
function createMerchant(payload) {
    return __awaiter(this, void 0, void 0, function* () {
        if (!payload)
            throw new Error("Payload is required for createMerchant");
        const res = yield httpClient.post("/api/merchant/create", payload);
        return res.data;
    });
}
/**
 * ✅ GET MERCHANT STATUS
 * Calls company backend /api/merchant/status
 */
function getMerchantStatus(token) {
    return __awaiter(this, void 0, void 0, function* () {
        var _a, _b;
        if (!token)
            throw new Error("Token is required for getMerchantStatus");
        const res = yield httpClient.get("/api/merchant/status", {
            headers: { Authorization: `Bearer ${token}` },
        });
        if (!((_a = res.data) === null || _a === void 0 ? void 0 : _a.success)) {
            throw new Error(((_b = res.data) === null || _b === void 0 ? void 0 : _b.message) || "Failed to fetch merchant status");
        }
        // 🔥 Return only actual merchant data
        return res.data.data;
    });
}
