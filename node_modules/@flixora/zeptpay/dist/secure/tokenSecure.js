"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getToken = getToken;
// src/secureServices/tokenService.ts
const axios_1 = __importDefault(require("axios"));
const keys_1 = require("../dev/keys");
let token = null;
let expiry = 0;
function getToken() {
    return __awaiter(this, void 0, void 0, function* () {
        const now = Date.now();
        if (token && now < expiry)
            return token;
        // Automatic refresh token call internally
        const res = yield axios_1.default.post("/api/merchant/refresh-token", { merchantId: keys_1.clientKey }, { headers: { "x-secret-key": keys_1.secretKey } });
        token = res.data.token;
        expiry = now + 15 * 60 * 1000; // 15 min example
        return token;
    });
}
