"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DevLogger = void 0;
class DevLogger {
    constructor(enabled = false) {
        this.enabled = enabled;
    }
    enable() {
        this.enabled = true;
    }
    disable() {
        this.enabled = false;
    }
    log(label, data) {
        if (!this.enabled)
            return;
        console.log(`[ZeptPay SDK] ${label}`, data ? JSON.stringify(data, null, 2) : "");
    }
    warn(label, data) {
        if (!this.enabled)
            return;
        console.warn(`[ZeptPay SDK ⚠️] ${label}`, data !== null && data !== void 0 ? data : "");
    }
    error(label, data) {
        if (!this.enabled)
            return;
        console.error(`[ZeptPay SDK ❌] ${label}`, data !== null && data !== void 0 ? data : "");
    }
}
exports.DevLogger = DevLogger;
