import { ZeptPayClient } from "../clients/ZeptPayClient";
export interface ZeptPayPlugin {
    name: string;
    install(client: ZeptPayClient): void;
}
export declare class PluginManager {
    private plugins;
    use(plugin: ZeptPayPlugin, client: ZeptPayClient): void;
    has(name: string): boolean;
    list(): string[];
}
