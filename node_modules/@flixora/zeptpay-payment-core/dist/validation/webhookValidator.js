"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateWebhookKeyFormat = void 0;
const validateWebhookKeyFormat = (key) => {
    if (!key)
        return false;
    // whsec- format required
    if (!key.startsWith("whsec-"))
        return false;
    // must contain ZeptPay ecosystem signature
    if (!key.includes("@zeptpay"))
        return false;
    // must contain ecosystem name
    if (!key.includes("flixora"))
        return false;
    // basic length safety check
    if (key.length < 40)
        return false;
    return true;
};
exports.validateWebhookKeyFormat = validateWebhookKeyFormat;
