import { ZeptPayClient } from "../clients/ZeptPayClient";
import { CreateAutoPayRequest, CreateAutoPayResponse, CaptureAutoPayRequest, CaptureAutoPayResponse } from "../types";
export declare class AutoPayModule {
    private client;
    constructor(client: ZeptPayClient);
    createAutoPayTransaction(data: CreateAutoPayRequest): Promise<CreateAutoPayResponse>;
    captureAutoPayPayment(data: CaptureAutoPayRequest): Promise<CaptureAutoPayResponse>;
}
