import { ZeptPayClient } from "../clients/ZeptPayClient";
export interface WebhookEvent {
    id: string;
    type: string;
    data: {
        object: any;
    };
    created: number;
    raw: any;
}
export interface ZeptPayWebhookInstance {
    verifyEventZPTFXO: (payload: any, signature: string, timestamp: string, webhookKey: string) => WebhookEvent;
    getStatus: (webhookKey: string) => Promise<any>;
}
export declare class ZeptPayWebhook {
    private static client;
    static init(client: ZeptPayClient): void;
    static verifyEventZPTFXO(payload: any, signature: string, timestamp: string, webhookKey: string): WebhookEvent;
    static getStatus(webhookKey: string): Promise<any>;
}
